# Brand Assets

The current GDIT official logo source is documented in `brand-intelligence.md`. The build environment could not materialize the remote binary reliably, so no unofficial traced or recreated mark is stored here. The campaign uses a clearly typeset company identifier as a documented technical fallback and does not claim it is the official logo.
